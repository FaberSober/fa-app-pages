import { Fa } from "@fa/ui";

export default {
  /** -------------------------------- 网关配置 -------------------------------- */
  GATE_APP: {
    /** app服务 */
    app: {
      app: '/api/app/app',
    },
  },
} as Fa.ConfigApp